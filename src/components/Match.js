import React from "react";
import { FormControl, Stack } from "@mui/material";

function Match({ handleChange }) {
  return (
    <FormControl>
      <Stack justifyContent="center" alignItems="center"></Stack>
    </FormControl>
  );
}

export default Match;
